---
argument_specs:
  main:
    short_description: "Configure network bonds and VLANs"
    options:
      unraid_network_setup_host:
        description: Target host address
        type: str
        required: true
      unraid_network_setup_username:
        description: Authentication username
        type: str
      unraid_network_setup_password:
        description: Authentication password
        type: str
      unraid_network_setup_api_key:
        description: API key for authentication
        type: str
      unraid_network_setup_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      unraid_network_setup_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
