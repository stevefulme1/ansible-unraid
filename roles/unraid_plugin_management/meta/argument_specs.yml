---
argument_specs:
  main:
    short_description: "Install and configure plugins"
    options:
      unraid_plugin_management_host:
        description: Target host address
        type: str
        required: true
      unraid_plugin_management_username:
        description: Authentication username
        type: str
      unraid_plugin_management_password:
        description: Authentication password
        type: str
      unraid_plugin_management_api_key:
        description: API key for authentication
        type: str
      unraid_plugin_management_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      unraid_plugin_management_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
