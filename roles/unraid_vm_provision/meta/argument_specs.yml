---
argument_specs:
  main:
    short_description: "Create and configure VMs"
    options:
      unraid_vm_provision_host:
        description: Target host address
        type: str
        required: true
      unraid_vm_provision_username:
        description: Authentication username
        type: str
      unraid_vm_provision_password:
        description: Authentication password
        type: str
      unraid_vm_provision_api_key:
        description: API key for authentication
        type: str
      unraid_vm_provision_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      unraid_vm_provision_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
