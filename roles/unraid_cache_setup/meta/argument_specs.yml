---
argument_specs:
  main:
    short_description: "Configure cache pools and mover settings"
    options:
      unraid_cache_setup_host:
        description: Target host address
        type: str
        required: true
      unraid_cache_setup_username:
        description: Authentication username
        type: str
      unraid_cache_setup_password:
        description: Authentication password
        type: str
      unraid_cache_setup_api_key:
        description: API key for authentication
        type: str
      unraid_cache_setup_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      unraid_cache_setup_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
