---
argument_specs:
  main:
    short_description: "Set up notifications and alerts"
    options:
      unraid_monitoring_host:
        description: Target host address
        type: str
        required: true
      unraid_monitoring_username:
        description: Authentication username
        type: str
      unraid_monitoring_password:
        description: Authentication password
        type: str
      unraid_monitoring_api_key:
        description: API key for authentication
        type: str
      unraid_monitoring_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      unraid_monitoring_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
